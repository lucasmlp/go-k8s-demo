apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-k8s-demo.fullname" . }}
  labels:
    {{- include "go-k8s-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "go-k8s-demo.selectorLabels" . | nindent 4 }}
